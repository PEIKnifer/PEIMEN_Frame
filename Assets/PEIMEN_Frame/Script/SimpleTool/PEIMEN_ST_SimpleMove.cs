using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using PEIKTS;
public class PEIMEN_ST_SimpleMove : PEIKnifer_ObjSimpleMove
{
    // Start is called before the first frame update
    void Start()
    {
        FrameInit();
    }

    // Update is called once per frame
    void Update()
    {
        FrameUpdate();
    }
}
